#include <bits/stdc++.h>
using namespace std;

#define pb push_back
#define f1(n) for(int i = 0;i<n;i++)
#define f2(n) for(int j = 0;j<n;j++)
#define mmax 100005
#define all(v) v.begin(), v.end()
#define ll long long

typedef pair<int, int> pi;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0), cout.tie(0);
    string d = "123_567";

    while(d != "567_123")
    {
        if(d[0] < 'd[3]' and d[3] - d[0] <=2)
    }
    return 0;
}